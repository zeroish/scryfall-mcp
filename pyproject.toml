[project]
name = "scryfall-mcp"
version = "0.1.0"
description = "MCP server wrapping the Scryfall API for EDH/Commander use cases"
requires-python = ">=3.11"
# Abstract deps — concrete pins live in requirements.txt (generated from requirements.in)
dependencies = [
    "fastmcp",
    "httpx",
]
